## 1. 3D Rack Elevation Spotlight Animation

- [x] 1.1 Update `RackMesh.tsx` scale and opacity target logic in `useFrame` to flatten unselected racks when a rack is selected

## 2. Verification

- [x] 2.1 Run `npx tsc --noEmit` and confirm zero TypeScript errors
