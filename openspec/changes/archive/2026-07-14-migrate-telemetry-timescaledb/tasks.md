## 1. Database Migration Setup (Flyway Integration)

- [x] 1.1 Add the Flyway dependency to `backend/build.gradle`: `implementation 'org.springframework.boot:spring-boot-starter-flyway'` and `implementation 'org.flywaydb:flyway-database-postgresql'`.
- [x] 1.2 Configure Flyway in `backend/src/main/resources/application.properties`:
  - Set `spring.jpa.hibernate.ddl-auto=validate` to enforce schema validation on startup.
  - Enable Flyway by verifying `spring.flyway.enabled=true` (default in Spring Boot once dependency is present).
- [x] 1.3 Create the baseline SQL migration file: `backend/src/main/resources/db/migration/V1__Baseline_Schema.sql`. This file must define all core tables as they are currently generated by Hibernate:
  - Tables: `room`, `rack`, `device_type`, `device`, `pdu`, `module_type`, `module`, `module_bay`, `console_port`, `power_port`, `interface`, and `equipment_alarm`.
  - Enforce foreign keys, unique constraints (e.g., room name uniqueness, rack name uniqueness within room), and indices (such as pg_trgm indices on device name/IP).
- [x] 1.4 Create the telemetry and TimescaleDB SQL migration file: `backend/src/main/resources/db/migration/V2__Timescale_Telemetry.sql`:
  - Enable TimescaleDB: `CREATE EXTENSION IF NOT EXISTS timescaledb;`
  - Create the `telemetry_logs` table with composite primary key `(id, timestamp)` using `id BIGINT GENERATED ALWAYS AS IDENTITY` and `timestamp TIMESTAMPTZ NOT NULL`.
  - Convert it to a hypertable: `SELECT create_hypertable('telemetry_logs', 'timestamp');`
  - Configure TimescaleDB compression on `telemetry_logs` segments: segment by `(device_id, metric_key)`.
  - Enable policies: add compression policy for `2 hours` and retention policy for `7 days`.

## 2. Docker Infrastructure — TimescaleDB Image

- [x] 2.1 In `docker-compose.yml`, replace the `postgres` service image from `postgres:16-alpine` to `timescaledb/timescaledb:latest-pg16`. Keep existing environment variables, ports, volumes, and healthcheck configurations.

## 3. Backend Entity and Repository Changes

- [x] 3.1 Update `TelemetryLog.java`:
  - Change `@GeneratedValue(strategy = GenerationType.SEQUENCE)` to `GenerationType.IDENTITY`.
  - Remove the `@SequenceGenerator` annotation.
  - Add `@Table` index definition: `@Index(name = "idx_telemetry_device_time", columnList = "deviceId, timestamp")`.
- [x] 3.2 Create `TelemetryHistoryDto.java` in the `features/telemetry/dto/` package as a Java record with fields:
  - `Instant bucket`
  - `String metricKey`
  - `Double avgValue`
  - `Double minValue`
  - `Double maxValue`
- [x] 3.3 Add a native query method to `TelemetryLogRepository` that queries historical telemetry logs:
  - Use TimescaleDB's `time_bucket()` to group logs by time interval, returning metrics matching `deviceId` between `from` and `to` timestamps.
  - Map the results to a list of historical metrics, matching the DTO contract.

## 4. Historical Telemetry API Endpoint

- [x] 4.1 Create `TelemetryHistoryController.java` in `features/telemetry/`:
  - Implement a GET endpoint: `/api/v1/telemetry/history/{deviceId}`.
  - Expose parameters: `from` (Instant), `to` (Instant), and `interval` (string default `'1 minute'`).
  - Add Swagger annotations matching the existing REST API documentation style.
- [x] 4.2 Add input validations to `TelemetryHistoryController`:
  - Verify that `from` is before `to`.
  - Restrict the maximum query duration (`to - from`) to 30 days to protect memory bounds.
  - Restrict `interval` to allowed time periods (`'30 seconds'`, `'1 minute'`, `'5 minutes'`, `'1 hour'`, `'1 day'`).

## 5. Verification and Cleanup

- [x] 5.1 Perform a clean reset of the local database container: run `docker compose down -v` to delete the postgres volume, then boot the stack with `docker compose up -d`.
- [x] 5.2 Verify that Flyway executes `V1__Baseline_Schema.sql` and `V2__Timescale_Telemetry.sql` successfully on startup. Check the database logs and verify tables using a database client or `docker exec postgres psql -U vdt_user -d vdt_db -c "\d"`.
- [x] 5.3 Trigger database seeding via the `/api/v1/seed` endpoint and verify that 10,000 devices are seeded without errors or degradation in speed.
- [x] 5.4 Verify that `TelemetrySchedulerService` collection cycles run, metrics are gathered in parallel via Virtual Threads, and logs are batch-saved into the TimescaleDB hypertable.
- [x] 5.5 Verify the history endpoint `/api/v1/telemetry/history/{deviceId}` returns time-bucketed values by making a query after the system has run for a few minutes.
